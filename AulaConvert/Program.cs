class Program{
    public static void Main(string[] args)
    {
        // é similar que o Parse, porém permite converter vários tipos de valor(não apenas string)
        int inteiro = Convert.ToInt32("100"); 
        Console.WriteLine(inteiro); 
    }
}